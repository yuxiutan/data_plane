# Layer A — Environment template
# Copy to .env and adjust as needed.
# cp .env.example .env

# Postgres
POSTGRES_USER=layera
POSTGRES_PASSWORD=layera
POSTGRES_DB=layera

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Optional: override Redpanda/EMQX/Logstash settings via env if needed later
# KAFKA_BOOTSTRAP=redpanda:9092
