# Grafana provisioning: load dashboard JSON from folder
apiVersion: 1
providers:
  - name: LayerA
    orgId: 1
    folder: Layer A
    folderUid: layera
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/dashboard-json
